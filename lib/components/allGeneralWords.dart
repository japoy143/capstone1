import 'package:flutter/material.dart';

class AllGeneralWords extends StatelessWidget {
  const AllGeneralWords({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('General Words'),
      ),
    );
  }
}
