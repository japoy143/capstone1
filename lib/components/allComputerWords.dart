import 'package:flutter/material.dart';

class AllComputerWords extends StatelessWidget {
  const AllComputerWords({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Computer Words Meaning'),
      ),
    );
  }
}
